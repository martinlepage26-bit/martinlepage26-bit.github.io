---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<ul class="flex flex-wrap gap-2" aria-label="Tags">
  {tags.map((tag) => <li class="border border-[rgba(36,27,23,0.12)] px-3 py-1 text-[0.76rem] font-mono uppercase tracking-[0.14em] text-slate-300">{tag}</li>)}
</ul>
